class SLoader
{
  public:
  SLoader();
  SLoader(const SLoader& other) = delete;
  SLoader(SLoader&& other) = delete;
  ~SLoader();

  SLoader& operator=(const SLoader& other) = delete;
  SLoader& operator=(SLoader&& other) = delete;

  private:
};
